# Copy this file to .env (do NOT commit .env)
# Local dev example for the webapi server

# Azure OpenAI / Inference key
AZURE_INFERENCE_SDK_KEY=your_azure_openai_key_here

# Azure OpenAI instance name (subdomain part before .openai.azure.com)
# For example, from https://<INSTANCE_NAME>.openai.azure.com
INSTANCE_NAME=your_instance_name_here

# Deployment name for the model (default/fallback)
DEPLOYMENT_NAME=gpt-4o

# Separate Deployments for Text vs Audio
# Text deployment: Standard GPT-4o/GPT-5 for ALL chat (text + voice intelligence)
TEXT_DEPLOYMENT_NAME=gpt-5
# Audio deployment: NO LONGER USED - Azure Speech Avatar replaces GPT-audio
# AUDIO_DEPLOYMENT_NAME=gpt-4o-audio

# Azure Speech Service (for Text-to-Speech Avatar with lip-sync)
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=westus2

# MCP Server URL
MCP_SERVER_URL=http://localhost:3001

# Optional: explicit endpoint if you need to set a custom one
#AZURE_OPENAI_ENDPOINT=https://<INSTANCE_NAME>.openai.azure.com
